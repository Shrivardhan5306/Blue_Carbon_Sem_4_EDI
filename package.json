{
  "name": "blue-carbon",
  "version": "1.0.0",
  "description": "Blue Carbon Root Project",
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix blockchain && cd backend/ai-backend && pip install -r requirements.txt",
    "start:backend": "cd backend/ai-backend && python -m uvicorn src.api:app --reload --port 8000",
    "start:frontend": "npm run dev --prefix frontend",
    "start:blockchain": "cd blockchain && npx hardhat node",
    "start:all": "npx concurrently \"npm run start:backend\" \"npm run start:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
